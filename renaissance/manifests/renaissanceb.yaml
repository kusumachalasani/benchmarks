apiVersion: apps/v1
kind: Deployment
metadata:
  name: renaissanceb-sample
  labels:
    app: renaissanceb-app
spec:
  replicas: 3
  selector:
   matchLabels:
     app: renaissanceb-deployment
  template:
    metadata:
      labels:
        name: renaissanceb-deployment
        app: renaissanceb-deployment
        # Add label to the application which is used by kruize/autotune to monitor it
        app.kubernetes.io/name: "renaissanceb-deployment"
        app.kubernetes.io/layer: "hotspot"
        version: v1
    spec:
      volumes:
      - name: test-volume
      containers:
      - name: renaissance-server
        image: prakalp23/renaissance1041:latest
        imagePullPolicy: IfNotPresent
        env:
        ports:
          - containerPort: 8080
        resources:
         requests:
         limits:
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: renaissanceb-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: '/metrics'
  labels:
    app: renaissanceb-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: renaissanceb-port
  selector:
    name: renaissanceb-deployment
