apiVersion: apps/v1
kind: Deployment
metadata:
  name: renaissancea-sample
  labels:
    app: renaissancea-app
spec:
  replicas: 2
  selector:
   matchLabels:
     app: renaissancea-deployment
  template:
    metadata:
      labels:
        name: renaissancea-deployment
        app: renaissancea-deployment
        # Add label to the application which is used by kruize/autotune to monitor it
        app.kubernetes.io/name: "renaissancea-deployment"
        app.kubernetes.io/layer: "hotspot"
        version: v1
    spec:
      volumes:
      - name: test-volume
      containers:
      - name: renaissance-server
        image: prakalp23/renaissance1041:latest
        imagePullPolicy: IfNotPresent
        env:
        ports:
          - containerPort: 8080
        resources:
         requests:
         limits:
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: renaissancea-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: '/metrics'
  labels:
    app: renaissancea-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: renaissancea-port
  selector:
    name: renaissancea-deployment
