apiVersion: apps/v1
kind: Deployment
metadata:
  name: renaissanced-sample-0
  labels:
    app: renaissanced-app-0
spec:
  replicas: 3
  selector:
   matchLabels:
     app: renaissanced-deployment
  template:
    metadata:
      labels:
        name: renaissanced-deployment
        app: renaissanced-deployment
        # Add label to the application which is used by kruize/autotune to monitor it
        app.kubernetes.io/name: "renaissanced-deployment"
        app.kubernetes.io/layer: "hotspot"
        version: v1
    spec:
      volumes:
      - name: test-volume
      containers:
      - name: renaissance-server
        image: kusumach/renaissance:0.14.1_j11
        imagePullPolicy: IfNotPresent
        env:
          - name: "TIME_LIMIT"
            value: "88400"
          - name: "BENCHMARKS"
            value: "future-genetic"
        ports:
          - containerPort: 8080
        resources:
         requests:
          cpu: 4
          memory: 4096M
         limits:
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: renaissanced-service-0
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: '/metrics'
  labels:
    app: renaissanced-app-0
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: renaissanced-port-0
  selector:
    name: renaissanced-deployment
