apiVersion: apps/v1
kind: Deployment
metadata:
  name: renaissancee-sample
  labels:
    app: renaissancee-app
spec:
  replicas: 3
  selector:
   matchLabels:
     app: renaissancee-deployment
  template:
    metadata:
      labels:
        name: renaissancee-deployment
        app: renaissancee-deployment
        # Add label to the application which is used by kruize/autotune to monitor it
        app.kubernetes.io/name: "renaissancee-deployment"
        app.kubernetes.io/layer: "hotspot"
        version: v1
    spec:
      volumes:
      - name: test-volume
      containers:
      - name: renaissance-server
        image: prakalp23/renaissance1041:latest
        imagePullPolicy: IfNotPresent
        env:
        ports:
          - containerPort: 8080
        resources:
         requests:
         limits:
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: renaissancee-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: '/metrics'
  labels:
    app: renaissancee-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: renaissancee-port
  selector:
    name: renaissancee-deployment
