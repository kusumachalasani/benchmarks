apiVersion: apps/v1
kind: Deployment
metadata:
  name: renaissancef-sample
  labels:
    app: renaissancef-app
spec:
  replicas: 3
  selector:
   matchLabels:
     app: renaissancef-deployment
  template:
    metadata:
      labels:
        name: renaissancef-deployment
        app: renaissancef-deployment
        # Add label to the application which is used by kruize/autotune to monitor it
        app.kubernetes.io/name: "renaissancef-deployment"
        app.kubernetes.io/layer: "hotspot"
        version: v1
    spec:
      volumes:
      - name: test-volume
      containers:
      - name: renaissance-server
        image: prakalp23/renaissance1041:latest
        imagePullPolicy: IfNotPresent
        env:
        ports:
          - containerPort: 8080
        resources:
         requests:
         limits:
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: renaissancef-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: '/metrics'
  labels:
    app: renaissancef-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: renaissancef-port
  selector:
    name: renaissancef-deployment
